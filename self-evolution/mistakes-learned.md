# 🎓 踩坑与教训

> 记录每个错误，避免重犯。

---

## 格式

```
### [分类]
- **简短描述**: 根因 → 修复/预防
```

---

### 记忆系统

- **短篇创作未落盘**（2026-05-11）  
  创作的《遗物整理师》未保存至本地文件，会话结束后丢失。  
  → **强制**：任何创作产物生成后立即写入 `knowledge-base/` 对应目录，引用永久文件路径而非会话记忆。

### 系统配置

- **Watchdog 注册项丢失**（2026-05-16）  
  计划任务被系统策略禁用 / 注册表 Run 项消失。  
  → 每次进化 cron 检查 `HKCU\Run` 中 `OpenClawWatchdog` 是否存在，丢失则重建。

### Token 管理

- **进化主流程不应由本地模型执行**（2026-05-17）  
  Cron 中指定 `ollama/qwen2.5:7b` 作执行模型，但 qwen 输出带 ANSI 乱码、推理不完整。  
  → 进化 cron 用 DeepSeek V4 Flash 执行，qwen2.5:7b 仅作辅助分析。
